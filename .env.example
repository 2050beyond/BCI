# DatoCMS API Token (Read-only for frontend)
# Get this from: https://dashboard.datocms.com/account/tokens
# Create a "Read-only" token for your project
NEXT_PUBLIC_DATOCMS_API_TOKEN=your_read_only_api_token_here

# DatoCMS Management API Token (Optional - only needed for setup scripts)
# Get this from: https://dashboard.datocms.com/account/tokens
# Create a "Full-access" token if you need to run setup scripts
DATOCMS_MANAGEMENT_API_TOKEN=your_management_api_token_here

# DatoCMS Environment (usually 'main')
DATOCMS_ENVIRONMENT=main
